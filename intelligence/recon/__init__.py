"""Recon intelligence pipeline (collect → analyze → score → assemble → report).

Import from submodule ``engine`` when running programmatically::

    from intelligence.recon.engine import run_recon_pipeline
"""
